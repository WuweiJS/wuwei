export default class Action extends Action {
  // Interface
  onFire(params) {
    // Override by user.

    /*
    transaction((Graph) => {
      // Synchronous use Graph in this block only.
    })
    */
  }
}
